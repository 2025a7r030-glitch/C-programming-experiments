#include<stdio.h>
int main() {
    int N, sum = 0;
    scanf("%d", &N);

    // Loop from 1 to N and add odd numbers to sum
    for (int i = 1; i <= N; i += 2) {
        sum += i;
    }

    printf("%d\n", sum);

    return 0;
}
